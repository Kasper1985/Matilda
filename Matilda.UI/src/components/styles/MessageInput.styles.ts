import { makeStyles } from "tss-react/mui";

export const messageInputStyles: any = makeStyles()(() => ({
    main: {
        display: 'flex',
        backgroundColor: '#444',
        borderRadius: '10px',
        padding: '15px 10px',
        alignItems: 'end',
    }
}));